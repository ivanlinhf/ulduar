version: "2"

sql:
  - engine: "postgresql"
    schema: "db/schema.sql"
    queries: "db/queries"
    gen:
      go:
        package: "dbsqlc"
        out: "internal/dbsqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
